# Pinned runtime for the SonicSIM semantic (CLAP) service — Step 2.
# Installed into its OWN venv at /opt/semantic-svc/.venv so it never perturbs
# the librosa venv on the same box.
#
# NOTE: this EC2 instance has no GPU (2 vCPU / 7GB), so torch is the CPU wheel.
# If you later move to a GPU box, swap the --index-url for the CUDA build that
# matches your NVIDIA driver and re-pin torch/torchaudio together.
#
# laion-clap is deliberately NOT listed here. Its metadata hard-pins
# numpy==1.23.5, which cannot build on Python 3.12 and conflicts with torch 2.4.
# install.sh installs it with --no-deps AFTER this file, and every runtime
# dependency it actually needs is pinned below instead.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0
pydantic==2.10.4
requests==2.32.3
numpy==1.26.4
torch==2.4.1
torchaudio==2.4.1
torchvision==0.19.1
librosa==0.10.2.post1
soundfile==0.12.1
transformers==4.44.2

# laion-clap runtime deps (installed here so --no-deps above is safe)
torchlibrosa==0.1.0
ftfy==6.2.3
braceexpand==0.1.7
webdataset==0.2.100
wget==3.2
progressbar==2.5
h5py==3.11.0
scikit-learn==1.5.2
pandas==2.2.3
scipy==1.13.1
tqdm==4.66.5
regex==2024.9.11
